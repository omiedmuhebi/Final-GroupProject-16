// Exporting database configuration
module.exports =
{
  // URI for the MongoDB connection
  // This is the connection string to the MongoDB database
  // The format is: mongodb://[hostname]/[database-name]
  // Here, '127.0.0.1' refers to the localhost, and 'shoppingCart' is the name of the database
  "URI": "mongodb://127.0.0.1:27017/shoppingCart"
}
