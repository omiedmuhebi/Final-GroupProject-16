<!-- Bootstrap navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-black navbar-custom"> <!-- Bootstrap's navbar class; 'navbar-expand-lg' ensures it's responsive; 'navbar-dark bg-black' sets the theme and background color -->
        <div class="container-fluid"> <!-- Bootstrap's container class for a fluid layout -->
            <a class="navbar-brand" href="/home"><i class="fa-brands fa-quora"></i>Cell</a> <!-- Navbar brand/logo, links to the home page -->

            <!-- Navbar toggler button for collapsed menu on smaller screens -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span> <!-- Icon for the toggler button -->
            </button>

            <!-- Collapsible navbar content -->
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0"> <!-- Navbar links in an unordered list; 'mx-auto' centers the navbar items -->

                    <!-- Home link in the navbar -->
                    <li class="nav-item">
                        <% if (title == 'Home') {%> <!-- Server-side script checking if the current page is 'Home' -->
                            <a class="nav-link active" aria-current="page" href="/home">Home</a> <!-- Active link for Home -->
                        <%} else { %> 
                            <a class="nav-link" aria-current="page" href="/home">Home</a> <!-- Not active link for Home -->
                        <% } %>
                    </li>

                    <!-- About link in the navbar -->
                    <li class="nav-item">
                        <% if (title == 'Products') {%> <!-- Server-side script checking if the current page is 'products' -->
                            <a class="nav-link active" aria-current="page" href="/products">Products</a> <!-- Active link for About -->
                        <%} else { %> 
                            <a class="nav-link" aria-current="page" href="/products">Products</a> <!-- Not active link for About -->
                        <% } %>
                    </li>

                    <!-- Projects link in the navbar -->
                    <li class="nav-item">
                        <% if (title == 'About') {%> <!-- Server-side script checking if the current page is 'About' -->
                            <a class="nav-link active" aria-current="page" href="/About">About</a> <!-- Active link for Projects -->
                        <%} else { %>
                            <a class="nav-link" aria-current="page" href="/About">About</a> <!-- Not active link for Projects -->
                        <% } %>
                    </li>
                    
                    <li class="nav-item">
                        <% if (title == 'Cart') {%> <!-- Server-side script checking if the current page is 'Cart' -->
                            <a class="nav-link active" aria-current="page" href="/items-list">Cart <i class="fa-solid fa-cart-shopping"></i></a> <!-- Active link for Projects -->
                        <%} else { %>
                            <a class="nav-link" aria-current="page" href="/items-list">Cart <i class="fa-solid fa-cart-shopping"></i></a> <!-- Not active link for Projects -->
                        <% } %>
                    </li>
                </ul>
            </div>
        </div>
    </nav>